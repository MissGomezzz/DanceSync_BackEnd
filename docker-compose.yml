services:
  postgres:
    image: postgres:16-alpine
    container_name: dancesync-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-dancesync}
      POSTGRES_USER: ${POSTGRES_USER:-dancesync}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dancesync}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-dancesync} -d ${POSTGRES_DB:-dancesync}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres-data:
